int main()
{
    //Logical operations:

    //== true if right and left sides are equal
    //!= true if both sides aren't equal
    //|| true if any side of the condition is true
    //&& true only if both conditions are true

    //Ternary operator (?)
    //x = (a == b) ? a : b
    //if a == b then x = a if not then x = b

    //Execute code within "if scope" if the condition in if(<condition>) is true
    if(1)
    {

    }
    //Execute if previous statement is false but this one is true 
    else if(1)
    {

    }
    //Execute if none of the previous ones are executed
    else
    {

    }

    int x = 5;
    //Switch statement
    switch(x)
    {
        //If x is 1
        case 1:
            //Any expression here
        break;

        //Gets executed since it's true
        case 5:
        break;

        //If none of the previous conditions are true
        default:
        break;   
    }

    return 0;
}